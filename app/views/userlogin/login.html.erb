<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Striped</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

    <%= javascript_include_tag 'https://js.stripe.com/v2/' %>

    <%= csrf_meta_tags %>
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
        <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
      <![endif]-->
  </head>
  <body>
    <!-- Add the top navigation bar-->
    <%= render 'shared/navigation_bar' %>

    <!--Divide the page into sidebar and main body -->
    <div class="container-fluid">
      <div class="row-fluid">
        <div class="col-xs-4 col-xs-offset-4">
          <!--Show flash messages here-->
          <%= form_tag '/userlogin/login', method: 'post', class: "form-horizontal center" do %>

          <!--Rendering flash messages-->
          <%= render 'shared/flash_alert_messages' if !flash.empty? %>
        <div class="form-group">
          <%= label_tag 'Email Id', nil ,class: "col-md-4 control-label" %>
          <div class="col-md-8">
            <%= text_field_tag :email_id, params[:email_id], class: "form-control"%>
          </div>
        </div>

        <div class="form-group">
          <%= label_tag "Password", nil, class: "col-md-4 control-label" %>
          <div class="col-md-8">
            <%= password_field_tag :password, params[:password], class: "form-control" %>
          </div>
        </div>

        <div class="form-group">
          <div class="col-xs-2 col-xs-offset-5">
            <%= submit_tag "Login", class: "btn btn-default btn-primary" %>
          </div>
        </div>
        <% end %>

        <hr/>

        <div class="col-md-12">
          New to Tezbazaari? <a href="/userlogin/add_user"><button type="button" class="btn btn-primary">Create your Tezbazaari Account</button></a>
        </div>

        </div>
      </div>
    </div>

    <%= yield %>

  </body>
</html>
